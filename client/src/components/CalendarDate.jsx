import styled from 'styled-components';

const CalendarDate = styled.td`
  width: 32px;
  height: 32px;
  padding: 4px;
  border-radius: 50%;
  text-align: center;
  cursor: default;
`;

export default CalendarDate;
